import React from "react";
import { View, Text, SafeAreaView } from "react-native";

const Decks = () => {
  return (
    <SafeAreaView>
      <View>
        <Text>Decks</Text>
      </View>
    </SafeAreaView>
  );
};

export default Decks;
